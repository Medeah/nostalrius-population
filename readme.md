Check Dependencies:
```
  node --version
  python3 --version
  sqlite3 --version
```
Install and run:
```
pip install -r requirements.txt
npm install
python3 createdb.py
python3 getpop.py
gulp javascript
python3 server.py
```
